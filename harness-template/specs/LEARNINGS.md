# LEARNINGS — erros resolvidos e lições (memória entre sessões)

> Registrar aqui erros novos e suas soluções ao final de cada feature
> (AGENTS.md seção 8). Ler no início de sessões de implementação.
>
> O que entra: erros não-óbvios que custaram tempo (comportamento real de API
> externa, pegadinhas de build/deploy, limitações do ambiente do agente,
> violações de processo e como evitá-las). O que NÃO entra: erro trivial de
> digitação, falha que o compilador aponta na hora.
>
> Formato: `AAAA-MM-DD — contexto do erro (literal, não parafraseado) —
> causa raiz — solução/regra daqui pra frente.`

- (vazio)
